# {{ ansible_managed }}

[libdefaults]
        default_realm = {{ samba_realm }}
        dns_lookup_realm = {{ samba_kerberos_dns_lookup_realm }}
        dns_lookup_kdc = {{ samba_kerberos_dns_lookup_kdc }}
