---
# defaults file for samba

samba_create_domain: yes

samba_realm: realm.example.com
samba_domain: realm
samba_netbios_name: dc
samba_admin_pass: Passw0rd
samba_dns_forwarder: 8.8.8.8

samba_kerberos_dns_lookup_realm: "false"
samba_kerberos_dns_lookup_kdc: "true"
